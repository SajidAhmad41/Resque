<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rapid Rescue Login</title>
    <style>
        /* General styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        body {
            background: #f0f4f8;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background: #f37a1f;
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 400px;
            width: 90%;
        }

        /* Logo */
        .logo img {
            width: 80px;
            margin-bottom: 10px;
        }

        /* Headings */
        h2, h1, p {
            color: #ffffff;
        }

        h1 {
            font-size: 20px;
            margin-bottom: 15px;
        }

        /* Google Login Button */
        .google-login {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            padding: 10px;
            background: #ffffff;
            border: 1px solid #f37a1f;
            border-radius: 5px;
            font-size: 14px;
            cursor: pointer;
            transition: 0.3s;
        }

        .google-login img {
            width: 20px;
            margin-right: 10px;
        }

        .google-login:hover {
            background: #e65c00;
        }

        /* Separator */
        .separator {
            margin: 15px 0;
            font-size: 14px;
            color: #ffffff;
        }

        /* Form */
        form {
            display: flex;
            flex-direction: column;
        }

        input {
            width: 100%;
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #e65c00;
            border-radius: 5px;
            font-size: 14px;
        }

        button[type="submit"] {
            width: 100%;
            padding: 10px;
            background: #4CAF50;
            color: #ffffff;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: 0.3s;
        }

        button[type="submit"]:hover {
            background: #45a049;
        }

        /* Signup Text */
        .signup-text {
            margin-top: 15px;
            font-size: 14px;
        }

        .signup-text a {
            color: #ffffff;
            text-decoration: none;
            font-weight: bold;
        }

        .signup-text a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">
            <img src="/images/login.jpg" alt="Rapid Rescue Logo">
        </div>
        <h2>Rapid Rescue</h2>
        <p>WELCOME 👋</p>
        <h1>Continue to your Account.</h1>

        <button class="google-login">
            <img src="/images/google.png" alt="Google Icon"> Log In with Google
        </button>

        <div class="separator">Or use Mobile Number</div>

        <form action="/login" method="POST">
            <input type="email" name="email" placeholder="Email" required>
            <input type="password" name="password" placeholder="Password" required>
            <button type="submit">Continue</button>
        </form>

        <p class="signup-text">Are you a Newbie? <a href="/signup">GET STARTED - IT'S FREE</a></p>
    </div>
</body>
</html>
